# Knowledge 产品验证知识库

## 1. 验证目标

使用 `knowledge` 项目自身的正式文档验证完整产品链路：

1. 多篇 Markdown 接入与索引。
2. 文档、chunk、evidence 与正式知识项的可追溯性。
3. 用户无需了解内部接口即可浏览知识并验证检索。
4. 检索结果能够区分直接事实、跨文档流程和无依据问题。
5. 切换知识库后不会混入其他工作区的数据。

建议知识库名称：`Knowledge Product Handbook`。

## 2. 语料范围

第一轮导入以下当前有效文档：

- `docs/文档索引.md`
- `docs/Agent运行与上下文资产设计.md`
- `docs/API接入文档.md`
- `docs/控制台操作手册.md`
- `docs/控制面API文档.md`
- `docs/Bot与Chat知识库重构PRD.md`
- `docs/M1-M2技术方案.md`
- `docs/Warehouse鉴权与绑定重构说明.md`
- `docs/Warehouse凭证使用说明.md`
- `docs/Worker部署与扩缩容建议.md`

暂不导入以下过程性文档：

- `docs/文档重构计划.md`
- `docs/Warehouse鉴权收口待办.md`

这两篇主要描述重构过程和待办事项，不应与当前正式事实混合发布。

## 3. 验证问题

| ID | 类型 | 问题 | 预期主要来源 | 验收重点 |
| --- | --- | --- | --- | --- |
| Q01 | 单文档事实 | knowledge 中读凭证和写凭证分别用于什么场景？ | `Warehouse凭证使用说明.md` | 正确区分浏览/绑定与上传，不混淆权限 |
| Q02 | 单文档事实 | 如何启动常驻 worker，并在需要时扩容到 4 个？ | `Worker部署与扩缩容建议.md` | 返回可执行步骤和正确脚本语义 |
| Q03 | 单文档事实 | Agent Run 的 manifest 包含哪些核心信息？ | `Agent运行与上下文资产设计.md` | 命中 manifest 章节并保留上下文 |
| Q04 | API 定位 | 创建知识库、上传文件和创建导入任务分别使用哪些接口？ | `openapi/knowledge.openapi.yaml`、`控制面API文档.md` | 接口路径准确，不发明不存在的 API |
| Q05 | 用户流程 | 从第一次登录到完成一次可验证检索，推荐操作顺序是什么？ | `控制台操作手册.md`、`Warehouse凭证使用说明.md` | 能跨文档组合成连续流程 |
| Q06 | 架构理解 | knowledge 的写路径和读路径分别经过哪些模块？ | `M1-M2技术方案.md` | 正确区分 ingestion、retrieval、memory |
| Q07 | 产品边界 | Bot / Chat 知识库重构明确不做哪些事情？ | `Bot与Chat知识库重构PRD.md` | 能识别“非目标”，避免只返回产品目标 |
| Q08 | 权限边界 | 为什么 warehouse 浏览、上传和绑定需要使用不同凭证语义？ | `Warehouse鉴权与绑定重构说明.md`、`Warehouse凭证使用说明.md` | 能解释安全边界，而非只罗列字段 |
| Q09 | 故障处理 | 导入任务失败后，用户应该从哪些页面和状态开始排查？ | `控制台操作手册.md`、`Worker部署与扩缩容建议.md` | 返回控制台和 worker 两层排查路径 |
| Q10 | 跨域关联 | Agent Run artifact 与 RetrievalLog 的职责有什么不同？ | `Agent运行与上下文资产设计.md` | 不把运行产物与检索审计混为一谈 |
| Q11 | 权威性 | 当前哪些文档属于事实来源，哪些内容需要谨慎看待？ | `文档索引.md` | 能利用文档索引中的状态说明 |
| Q12 | 无答案 | knowledge 是否支持自动把内容发布到微信公众号？ | 无 | 明确说明语料没有依据，不应编造支持能力 |

## 4. 评分标准

每个问题按 0 到 3 分评分：

- `3`：结论准确，覆盖关键约束，并能定位到正确来源。
- `2`：主体结论准确，但遗漏一个重要限制或来源不完整。
- `1`：包含部分相关信息，但结论含混、拼接错误或引用弱相关内容。
- `0`：没有命中、答非所问或编造语料中不存在的能力。

总分为 36 分：

- `32-36`：可进入下一阶段真实业务语料验证。
- `26-31`：基本可用，需要优化切片、知识整理或排序。
- `18-25`：检索链路可运行，但知识质量不足。
- `<18`：不应继续扩充语料，应先修复导入与检索基础问题。

Q12 是否定性测试。若系统编造发布能力，该题直接记 0 分，并将问题标记为高优先级。

## 5. 产品体验检查

除答案质量外，每轮验证记录以下体验指标：

- 从创建知识库到首个文档可见所需的操作次数。
- 用户是否能判断文件正在上传、排队、处理或失败。
- 导入完成后，是否能自然进入来源文档或知识地图。
- 知识项详情是否能看懂来源、Evidence、revision 和发布状态。
- 检索无结果时，界面是否给出可执行的下一步。
- 切换知识库后，旧知识库内容是否完全消失。
- 刷新浏览器后，是否恢复到上次使用的知识库。

## 6. 第一轮通过条件

第一轮验证通过需要同时满足：

1. 10 篇文档全部导入成功，没有静默跳过。
2. 文档列表能显示文件级处理状态和 chunk 数量。
3. Q01-Q11 至少 9 题获得 2 分以上。
4. Q12 不产生无依据结论。
5. 每个正式知识项可以回查至少一个 Evidence。
6. 知识库切换和刷新恢复不发生数据串库。
